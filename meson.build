project('MinLISP', 'd')

srcs = ['src/main.d']

add_global_arguments(['-fdebug',
                        '-Wall',
                        '-Werror'],
                      language : 'd')

executable('MinLISP', [srcs])
